{
  "name": "lll",
  "version": "0.2.0",
  "private": true,
  "description": "LifeLongLearn — local learning workbench (Go backend + static frontend).",
  "scripts": {
    "build": "go build -o dist/lll.exe ./backend-go/cmd/lll",
    "run": "go run ./backend-go/cmd/lll",
    "start": "dist/lll.exe",
    "test": "go test ./...",
    "test:verbose": "go test -v ./..."
  },
  "engines": {
    "go": ">=1.22"
  }
}
