# Database Design

This iteration has no persistent database.

## Runtime State

Current state is held in memory:

```text
tasks map
task logs array per task
result text per task
```

## Follow-Up

Persistence will be designed in a later iteration after task and artifact lifecycles stabilize.
