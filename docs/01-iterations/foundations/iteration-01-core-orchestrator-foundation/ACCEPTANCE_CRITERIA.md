# Acceptance Criteria

```text
Project exists in D:\2_Study\LLL
Git repository is initialized
README, AGENTS, CLAUDE, and docs index exist
Server starts with npm start
Browser console loads from localhost:8787
Task monitor shows running, failed, completed, and cancelled states
Rendered mode can display Markdown and Mermaid output
```
