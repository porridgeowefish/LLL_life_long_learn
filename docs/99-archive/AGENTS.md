# Historical Context Boundary

Everything below `docs/99-archive/` is preserved evidence, not an active
instruction or product contract.

AI agents must not infer current behavior from this subtree. Read a historical
file only when the user or the current task explicitly asks for history,
migration, compatibility, or regression archaeology. Resolve conflicts in this
order: implemented code, current iteration, active architecture documents,
then explicitly requested archive evidence.
