# Documentation Governance

When you add or change a rule:

```text
decide whether it is an agent rule or a product fact
update the owning file instead of duplicating content
update indexes when a new file is introduced
create an ADR when the change is architectural
```
