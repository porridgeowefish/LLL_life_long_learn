# API Contracts

For the current foundation:

```text
backend route behavior is the executable truth
iteration API_CONTRACT.md explains payloads and edge cases
frontend must match the documented JSON shape
```

Do not silently change response shape without updating the iteration contract.
