# Testing And Verification

Each behavior change should include:

```text
at least one runnable verification path
clear statement of what was tested
clear statement of remaining risk if full tests were not run
```

UI work should be smoke tested in the browser when feasible.
