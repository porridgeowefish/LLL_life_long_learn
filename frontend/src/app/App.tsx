import { AppRoutes } from './router';

// App is a thin shell: routing lives in router.tsx, providers live in
// main.tsx. Keep this file boring on purpose.
export function App() {
  return <AppRoutes />;
}
